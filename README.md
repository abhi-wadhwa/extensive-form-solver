# extensive-form-solver

game trees. backward induction. sequence-form LPs for when converting to normal form would cost you more memory than the insight is worth.

von stengel (1996) figured out you could work with realization plans instead of pure strategies. i just implemented it and added some pretty trees.
